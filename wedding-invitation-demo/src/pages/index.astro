---
import fs from 'node:fs';
import path from 'node:path';
import '../styles/global.css';

// public/image 폴더 내의 이미지 파일 목록을 자동으로 읽어옵니다.
const imageDir = path.resolve('./public/image');
let images = [];
try {
  images = fs.readdirSync(imageDir)
    .filter(file => /\.(jpe?g|png|gif|webp)$/i.test(file))
    .sort((a, b) => {
      // 숫자로 된 파일명(1.jpg, 2.jpg 등)을 자연스럽게 정렬합니다.
      const numA = parseInt(a);
      const numB = parseInt(b);
      if (!isNaN(numA) && !isNaN(numB)) return numA - numB;
      return a.localeCompare(b);
    });
} catch (e) {
  console.error('이미지 폴더를 읽는 중 오류 발생:', e);
}
---

<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
		<!-- Referer를 무조건 보내도록 설정 (네이버 지도 인증 필수) -->
		<meta name="referrer" content="always">
		<title>이찬희 ♥ 원대연 결혼식</title>
		<!-- 이미지 사전 로드 -->
		<link rel="preload" as="image" href="/chungchup/play_timed.gif" />
		<link rel="preload" as="image" href="/chungchup/image/42.jpg" />
		
		<meta name="description" content="이찬희와 원대연의 결혼식에 초대합니다" />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Hahmlet:wght@300;500;700&family=Yeon+Sung&family=Nanum+Pen+Script&display=swap" rel="stylesheet">
		<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/gh/moonspam/NanumSquare@2.0/nanumsquare.css">
		<!-- 카카오맵 SDK -->
		<script is:inline type="text/javascript" src="https://dapi.kakao.com/v2/maps/sdk.js?appkey=51211e1cc3ff7894096e02e1de129d3d&libraries=services"></script>
		<!-- 네이버 지도 SDK -->
		<script is:inline type="text/javascript" src="https://oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=oya3z2iso0"></script>
		<script is:inline>
			window.navermap_authFailure = function () {
				console.error("Naver Map Auth Failure");
			};
		</script>
	</head>
	<body class="text-gray-900 font-sans mx-auto shadow-2xl" style="max-width: 448px; height: 100dvh; background-color: #f9fafb; position: relative; overflow: hidden; overscroll-behavior: none;">
		<!-- 흩날리는 꽃잎 배경 -->
		<div id="petal-container" aria-hidden="true" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0; overflow: hidden;"></div>
		
		<!-- 커튼 효과 요소들 -->
		<div id="curtain-overlay" class="absolute inset-0 z-50 pointer-events-none" data-curtain="true" style="display: block; width: 100%; height: 100%; overflow: hidden;">
			<div id="black-overlay-container" class="absolute inset-0 z-49 pointer-events-none" style="display: block; width: 100%; height: 100%; opacity: 1; will-change: opacity;">
				<svg width="100%" height="100%" preserveAspectRatio="none">
					<defs>
						<mask id="spotlight-mask">
							<rect width="100%" height="100%" fill="white" />
							<path id="spotlight-path" d="" fill="black" />
						</mask>
					</defs>
					<rect width="100%" height="100%" fill="black" opacity="0.97" mask="url(#spotlight-mask)" />
				</svg>
			</div>
			<div id="initial-overlay" class="absolute bg-white" style="z-index: 50; display: block; position: absolute; inset: 0; background-color: white; opacity: 0;"></div>
			<div id="curtain-top-left" class="absolute bg-white" style="z-index: 51; display: block; position: absolute; background-color: white;"></div>
			<div id="curtain-top-right" class="absolute bg-white" style="z-index: 51; display: block; position: absolute; background-color: white;"></div>
			<div id="curtain-bottom-left" class="absolute bg-white" style="z-index: 51; display: block; position: absolute; background-color: white;"></div>
			<div id="curtain-bottom-right" class="absolute bg-white" style="z-index: 51; display: block; position: absolute; background-color: white;"></div>
			<div id="initial-gray-line-left" class="absolute bg-gray-300" style="z-index: 53; display: block; height: 1px; opacity: 0;"></div>
			<div id="initial-gray-line-right" class="absolute bg-gray-300" style="z-index: 53; display: block; height: 1px; opacity: 0;"></div>
			<svg id="wrinkles-container" style="z-index: 52; display: block; position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none;"></svg>
		</div>

		<!-- 메인 컨테이너 -->
		<main id="notebook-container" class="notebook-container h-full flex flex-col mx-auto" style="padding: 30px 10px 10px 10px; max-width: 448px; position: relative; z-index: 10; opacity: 0; pointer-events: none; box-sizing: border-box;">

			<!-- 상세보기 모달 -->
			<div id="contact-modal" class="absolute inset-0 z-[100] flex items-center justify-center opacity-0 pointer-events-none transition-opacity duration-300 rounded-[30px]" style="background-color: rgba(0,0,0,0.8); margin: 30px 10px 10px 10px;">
				<div class="absolute top-6 left-6 text-white text-3xl cursor-pointer p-2 z-10" id="modal-close-btn">&times;</div>
				<div id="modal-content-box" class="bg-white w-[85%] max-w-[300px] rounded-2xl p-6 shadow-2xl transform scale-90 transition-transform duration-300">
					<h4 id="modal-title" class="text-lg font-bold text-gray-900 border-b-2 border-gray-100 pb-2 mb-4 text-center">신랑 이찬희</h4>
					<div class="space-y-4">
						<div>
							<p class="text-[10px] text-gray-400 mb-1">연락처</p>
							<div class="flex justify-between items-center bg-gray-50 p-2.5 rounded-lg">
								<span id="modal-phone" class="text-base font-semibold text-gray-800">010-1234-5678</span>
								<button class="modal-copy-btn bg-gray-900 text-white text-[10px] font-bold px-2 py-1 rounded active:scale-95" data-copy-target="modal-phone">복사</button>
							</div>
						</div>
						<div>
							<p class="text-[10px] text-gray-400 mb-1">계좌번호</p>
							<div class="flex flex-col bg-gray-50 p-2.5 rounded-lg gap-2">
								<div class="flex justify-between items-center">
									<span id="modal-account" class="text-sm font-semibold text-gray-800 leading-tight">국민 123-45-67890</span>
									<button class="modal-copy-btn bg-gray-900 text-white text-[10px] font-bold px-2 py-1 rounded active:scale-95" data-copy-target="modal-account">복사</button>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>

			<!-- 텍스트 전용 상세보기 모달 (눈이 안 좋으신 분들을 위한 큰 글씨) -->
			<div id="text-modal" class="absolute inset-0 z-[110] flex items-center justify-center opacity-0 pointer-events-none transition-opacity duration-300 rounded-[30px]" style="background-color: rgba(0,0,0,0.8); margin: 30px 10px 10px 10px;">
				<div class="absolute top-6 left-6 text-white text-3xl cursor-pointer p-2 z-10" id="text-modal-close-btn">&times;</div>
				<div id="text-modal-content-box" class="bg-white w-[90%] max-w-[350px] rounded-2xl p-8 shadow-2xl transform scale-90 transition-transform duration-300 max-h-[70%] overflow-y-auto">
					<h4 id="text-modal-title" class="text-xl font-bold text-gray-900 border-b-2 border-gray-100 pb-3 mb-6 text-center">안내</h4>
					<div id="text-modal-body" class="text-[clamp(1.2rem,5vw,1.6rem)] text-gray-800 leading-relaxed text-center font-medium" style="font-family: 'Hahmlet', serif; word-break: keep-all; overflow-wrap: break-word;">
						내용이 여기에 표시됩니다.
					</div>
				</div>
			</div>

			<div class="notebook-rings">
				<div class="ring"></div>
				<div class="ring"></div>
				<div class="ring"></div>
				<div class="ring"></div>
				<div class="ring"></div>
			</div>

			<div id="tab-content" class="notebook-page flex-1" style="perspective: 1500px; position: relative;">

				<!-- 표지 페이지 -->
				<div id="tab-cover" class="tab-panel active" style="z-index: 5; position: relative; background: linear-gradient(135deg, #e3c1a1 0%, #d2b48c 100%); box-shadow: inset 0 0 80px rgba(0,0,0,0.15), inset 0 0 20px rgba(0,0,0,0.1); border: 3px solid #5d4037;">
					<div class="absolute inset-0 opacity-30 pointer-events-none" style="background-image: url('data:image/svg+xml,%3Csvg viewBox=\"0 0 200 200\" xmlns=\"http://www.w3.org/2000/svg\"%3E%3Cfilter id=\"noiseFilter\"%3E%3CfeTurbulence type=\"fractalNoise\" baseFrequency=\"0.65\" numOctaves=\"3\" stitchTiles=\"stitch\"/%3E%3C/filter%3E%3Crect width=\"100%25\" height=\"100%25\" filter=\"url(%23noiseFilter)\"/%3E%3C/svg%3E');"></div>
					<img src="/chungchup/play_timed.gif" alt="play animation" style="width: 85%; height: auto; object-fit: contain; border-radius: 10px; position: absolute; bottom: 3%; left: 50%; transform: translateX(-50%); z-index: 10;" />
				</div>

				<!-- 탭 1: 소개/인사 -->
				<div id="tab-home" class="tab-panel hidden-under flex flex-col" data-page="0">
					<section class="flex-1 px-2 pt-2 pb-1 flex flex-col overflow-hidden">
						<div class="flex-1 relative w-full rounded-2xl overflow-hidden shadow-sm border border-gray-100 bg-gray-50 cursor-pointer">
							<div id="main-photo" class="absolute inset-0 w-full h-full bg-cover bg-center transition-all duration-500" style="background-image: url('/chungchup/image/42.jpg');"></div>
							<div class="relative z-10 text-center pt-2 pb-4 px-4 h-full flex flex-col pointer-events-none">
								<div class="mb-2">
									<h1 class="text-[clamp(1.8rem,9vw,2.25rem)] font-bold text-gray-900 mb-1 drop-shadow-md whitespace-nowrap" style="font-family: 'Yeon Sung', cursive; letter-spacing: -0.05em; -webkit-text-stroke: 0.1px white; text-shadow: 0 0 1px white;">
										이찬희 <span style="color: inherit;">♥</span> 원대연
									</h1>
								</div>
								<div class="mt-auto bg-white/60 backdrop-blur-[3px] py-4 rounded-xl border border-white/30">
									<h2 class="text-[clamp(1.5rem,7vw,1.8rem)] font-bold text-gray-900 mb-0 whitespace-nowrap" style="font-family: 'Nanum Pen Script', cursive;">
										2026. 08. 22. (토) 오전 11:00
									</h2>
									<div id="d-day-display" class="text-[clamp(2.0rem,9vw,2.4rem)] font-bold text-gray-900" style="font-family: 'Nanum Pen Script', cursive;">
										D-DAY
									</div>
								</div>
							</div>
						</div>
					</section>

					<section class="px-2 pb-2 pt-1 text-center flex-shrink-0">
						<div id="intro-box" class="bg-gray-50 rounded-2xl p-3 shadow-sm border border-gray-100 h-[115px] flex flex-col justify-center cursor-pointer hover:bg-gray-100 transition-colors">
							<div class="text-[clamp(11px,3.5vw,13px)] text-gray-800 leading-relaxed flex flex-col items-center font-medium" style="font-family: 'NanumSquare', sans-serif; letter-spacing: -0.03em;">
								<span class="whitespace-nowrap">우연히 시작된 만남은 적잖은 필연이 되었습니다.</span>
								<span class="whitespace-nowrap">그 필연을 켜켜이 포개보니 운명임을 알았습니다.</span>
								<span class="whitespace-nowrap">그러니 서로 함께 하는것이 행복임을 알겠습니다.</span>
								<span class="whitespace-nowrap">그 다짐의 기쁨을 여러분과 나누려하니</span>
								<span class="whitespace-nowrap">빛나는 축복을 선물해주시길 부탁드립니다.</span>
							</div>
						</div>
					</section>
				</div>

				<!-- 탭 2: 사진첩 -->
				<div id="tab-gallery" class="tab-panel hidden-under" data-page="1">
					<section id="gallery-section" class="px-4 py-4 flex flex-col h-full overflow-hidden relative">
						<h3 class="text-lg font-semibold text-center mb-4 text-gray-900 flex-shrink-0">사진첩</h3>
						
						<div class="flex-1 overflow-y-auto pb-8 scrollbar-hide">
							<div class="grid grid-cols-3 gap-2 pt-2">
								{images.map(img => (
									<div class="aspect-square rounded-md overflow-hidden shadow-sm border border-gray-100">
										<img src={`/chungchup/image/${img}`} class="w-full h-full object-cover" loading="lazy" />
									</div>
								))}
							</div>
						</div>
					</section>
				</div>

				<!-- 탭 3: 오시는 길 -->
				<div id="tab-location" class="tab-panel hidden-under" data-page="2">
					<section class="px-4 pt-4 pb-0">
						<h3 class="text-lg font-semibold text-center mb-4 text-gray-900">오시는 길</h3>
						<div id="venue-box" class="text-center mb-4 cursor-pointer hover:bg-gray-50 rounded-xl p-2 transition-colors">
							<h4 class="text-base font-bold mb-1">아이티 컨벤션</h4>
							<p class="text-sm text-gray-600 mb-1">흥덕IT밸리 2층 그레이스 홀</p>
							<p class="text-xs text-gray-500">경기도 용인시 기흥구 흥덕1로 13</p>
						</div>
						<div class="relative w-full rounded-t-lg overflow-hidden border border-gray-200 bg-gray-50" style="height: 224px;">
							<div id="map-kakao" class="map-view absolute inset-0 z-10" style="display: block; width: 100%; height: 100%;"></div>
							<div id="map-naver" class="map-view absolute inset-0 z-0" style="display: none; width: 100%; height: 100%;"></div>
							<div id="map-google" class="map-view absolute inset-0 z-0" style="display: none; width: 100%; height: 100%;">
								<iframe src="https://maps.google.com/maps?q=아이티컨벤션&t=&z=15&ie=UTF8&iwloc=&output=embed" class="w-full h-full border-0"></iframe>
							</div>
						</div>
						<div class="flex space-x-1 mb-1.5 px-1 mt-[-1px]">
							<button class="map-tab active flex-1 py-2 text-[11px] font-bold rounded-b-lg transition-all" data-map="kakao">KAKAOMAP</button>
							<button class="map-tab flex-1 py-2 text-[11px] font-bold rounded-b-lg transition-all" data-map="naver">NAVERMAP</button>
							<button class="map-tab flex-1 py-2 text-[11px] font-bold rounded-b-lg transition-all" data-map="google">GOOGLEMAP</button>
						</div>

						<a id="route-btn" href="https://map.kakao.com/link/to/아이티컨벤션,37.2709214,127.0661671" target="_blank" class="block mx-1 py-3 bg-gray-800 text-white text-center rounded-lg font-bold shadow-md hover:bg-gray-900 transition-colors mb-1.5 no-underline">
							길찾기
						</a>
						
						<!-- 교통 정보 및 말풍선 -->
						<div class="relative px-1 mb-4" id="trans-tab-container">
							<!-- 말풍선 (Tooltip) -->
							<div id="trans-balloon" class="absolute left-0 bottom-full mb-2 w-full opacity-0 pointer-events-none transition-all duration-300 translate-y-2 z-[60] cursor-pointer">
								<div class="bg-gray-900 text-white rounded-xl p-4 shadow-xl relative text-center mx-auto" style="width: 95%;">
									<div id="balloon-content" class="text-[11px] leading-relaxed">정보가 여기에 표시됩니다.</div>
									<div class="text-[9px] text-gray-400 mt-2 font-light">💡 클릭하면 크게 보입니다</div>
									<!-- 말풍선 꼬리 -->
									<div id="balloon-tail" class="absolute -bottom-2 left-1/2 -translate-x-1/2 w-0 h-0 border-l-[8px] border-l-transparent border-r-[8px] border-r-transparent border-t-[8px] border-t-gray-900"></div>
								</div>
							</div>

							<div class="flex border border-gray-200 rounded-lg overflow-hidden bg-white shadow-sm">
								<button class="trans-tab flex-1 py-2.5 text-[11px] font-bold transition-all hover:bg-gray-50 active:bg-gray-100" data-trans="shuttle">셔틀버스</button>
								<button class="trans-tab flex-1 py-2.5 text-[11px] font-bold border-l border-gray-200 transition-all hover:bg-gray-50 active:bg-gray-100" data-trans="subway">지하철</button>
								<button class="trans-tab flex-1 py-2.5 text-[11px] font-bold border-l border-gray-200 transition-all hover:bg-gray-50 active:bg-gray-100" data-trans="parking">주차안내</button>
							</div>
						</div>
					</section>

					<section class="px-4 py-4 mt-2">
						<div class="space-y-1.5" id="transport-accordion-placeholder">
						</div>
					</section>
				</div>

				<!-- 탭 4: 연락처 -->
				<div id="tab-heart" class="tab-panel hidden-under" data-page="3">
					<section class="px-2 py-4 h-full flex flex-col overflow-y-auto scrollbar-hide">
						<h3 class="text-lg font-semibold text-center mb-6 text-gray-900">연락처</h3>
						
						<div class="grid grid-cols-2 gap-2 flex-1">
							<!-- 신랑측 -->
							<div class="space-y-1.5">
								<div class="text-center font-bold text-sm py-1 bg-gray-100 rounded mb-2">신랑측</div>
								
								<div class="contact-card cursor-pointer bg-white p-2 rounded-lg border border-gray-100 shadow-sm space-y-2 hover:bg-gray-50 transition-colors">
									<div class="info-name text-xs font-bold text-gray-900 border-b pb-1">신랑 이찬희</div>
									<div class="flex flex-col gap-1">
										<div class="flex justify-between items-center bg-gray-50 p-1.5 rounded">
											<span class="info-phone text-[9px] text-gray-500">010-8794-5755</span>
											<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="010-8794-5755">복사</button>
										</div>
										<div class="flex flex-col bg-gray-50 p-1.5 rounded">
											<div class="flex justify-between items-center mb-0.5">
												<span class="info-account text-[9px] text-gray-800 font-medium">신한 110-392-817947</span>
												<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="110-392-817947">복사</button>
											</div>
										</div>
									</div>
								</div>

								<div class="contact-card cursor-pointer bg-white p-2 rounded-lg border border-gray-100 shadow-sm space-y-2 hover:bg-gray-50 transition-colors">
									<div class="info-name text-xs font-bold text-gray-900 border-b pb-1">부 김○○</div>
									<div class="flex flex-col gap-1">
										<div class="flex justify-between items-center bg-gray-50 p-1.5 rounded">
											<span class="info-phone text-[9px] text-gray-500">010-0000-0000</span>
											<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="010-0000-0000">복사</button>
										</div>
										<div class="flex flex-col bg-gray-50 p-1.5 rounded">
											<div class="flex justify-between items-center mb-0.5">
												<span class="info-account text-[9px] text-gray-800 font-medium">우리 1002-000-000</span>
												<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="1002-000-000">복사</button>
											</div>
										</div>
									</div>
								</div>

								<div class="contact-card cursor-pointer bg-white p-2 rounded-lg border border-gray-100 shadow-sm space-y-2 hover:bg-gray-50 transition-colors">
									<div class="info-name text-xs font-bold text-gray-900 border-b pb-1">모 박○○</div>
									<div class="flex flex-col gap-1">
										<div class="flex justify-between items-center bg-gray-50 p-1.5 rounded">
											<span class="info-phone text-[9px] text-gray-500">010-1111-1111</span>
											<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="010-1111-1111">복사</button>
										</div>
										<div class="flex flex-col bg-gray-50 p-1.5 rounded">
											<div class="flex justify-between items-center mb-0.5">
												<span class="info-account text-[9px] text-gray-800 font-medium">신한 110-000-000</span>
												<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="110-000-000">복사</button>
											</div>
										</div>
									</div>
								</div>
							</div>

							<!-- 신부측 -->
							<div class="space-y-1.5">
								<div class="text-center font-bold text-sm py-1 bg-gray-100 rounded mb-2">신부측</div>
								
								<div class="contact-card cursor-pointer bg-white p-2 rounded-lg border border-gray-100 shadow-sm space-y-2 hover:bg-gray-50 transition-colors">
									<div class="info-name text-xs font-bold text-gray-900 border-b pb-1">신부 원대연</div>
									<div class="flex flex-col gap-1">
										<div class="flex justify-between items-center bg-gray-50 p-1.5 rounded">
											<span class="info-phone text-[9px] text-gray-500">010-5678-9012</span>
											<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="010-5678-9012">복사</button>
										</div>
										<div class="flex flex-col bg-gray-50 p-1.5 rounded">
											<div class="flex justify-between items-center mb-0.5">
												<span class="info-account text-[9px] text-gray-800 font-medium">신한 987-65-43210</span>
												<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="987-65-43210">복사</button>
											</div>
										</div>
									</div>
								</div>

								<div class="contact-card cursor-pointer bg-white p-2 rounded-lg border border-gray-100 shadow-sm space-y-2 hover:bg-gray-50 transition-colors">
									<div class="info-name text-xs font-bold text-gray-900 border-b pb-1">부 이○○</div>
									<div class="flex flex-col gap-1">
										<div class="flex justify-between items-center bg-gray-50 p-1.5 rounded">
											<span class="info-phone text-[9px] text-gray-500">010-2222-2222</span>
											<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="010-2222-2222">복사</button>
										</div>
										<div class="flex flex-col bg-gray-50 p-1.5 rounded">
											<div class="flex justify-between items-center mb-0.5">
												<span class="info-account text-[9px] text-gray-800 font-medium">하나 000-0000-000</span>
												<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="000-0000-000">복사</button>
											</div>
										</div>
									</div>
								</div>

								<div class="contact-card cursor-pointer bg-white p-2 rounded-lg border border-gray-100 shadow-sm space-y-2 hover:bg-gray-50 transition-colors">
									<div class="info-name text-xs font-bold text-gray-900 border-b pb-1">모 최○○</div>
									<div class="flex flex-col gap-1">
										<div class="flex justify-between items-center bg-gray-50 p-1.5 rounded">
											<span class="info-phone text-[9px] text-gray-500">010-3333-3333</span>
											<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="010-3333-3333">복사</button>
										</div>
										<div class="flex flex-col bg-gray-50 p-1.5 rounded">
											<div class="flex justify-between items-center mb-0.5">
												<span class="info-account text-[9px] text-gray-800 font-medium">기업 000-000-000</span>
												<button class="copy-btn text-[8px] font-bold bg-white border border-gray-200 px-1 rounded" data-copy="000-000-000">복사</button>
											</div>
										</div>
									</div>
								</div>
							</div>
						</div>

					</section>
				</div>

			</div>

			<nav id="tab-bar" class="flex-shrink-0 bg-white border-t border-gray-100 safe-area-bottom" style="margin: 0 10px;">
				<div class="flex justify-around items-center h-16">
					<button class="tab-btn active flex flex-col items-center justify-center flex-1 h-full" data-tab="home">
						<span class="text-[clamp(14px,4.5vw,18px)] font-bold leading-[1.2] tracking-tighter">소개</span>
					</button>
					<button class="tab-btn flex flex-col items-center justify-center flex-1 h-full" data-tab="gallery">
						<span class="text-[clamp(14px,4.5vw,18px)] font-bold leading-[1.2] tracking-tighter">사진첩</span>
					</button>
					<button class="tab-btn flex flex-col items-center justify-center flex-1 h-full" data-tab="location">
						<span class="text-[clamp(14px,4.5vw,18px)] font-bold leading-[1.2] tracking-tighter">오시는 길</span>
					</button>
					<button class="tab-btn flex flex-col items-center justify-center flex-1 h-full" data-tab="heart">
						<span class="text-[clamp(14px,4.5vw,18px)] font-bold leading-[1.2] tracking-tighter">연락처</span>
					</button>
				</div>
			</nav>

		</main>

		<style is:global>
			#petal-container {
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				pointer-events: none;
				z-index: 20;
				overflow: hidden;
			}
			.petal {
				position: absolute;
				background: rgba(230, 230, 230, 0.5);
				border-radius: 40% 60% 60% 40% / 60% 40% 60% 40%;
				animation-name: fall;
				animation-timing-function: linear;
				animation-iteration-count: infinite;
			}
			@keyframes fall {
				from { transform: translateY(-20vh) translateX(var(--sway-start)) rotate(var(--rotate-start)); }
				to { transform: translateY(110vh) translateX(var(--sway-end)) rotate(var(--rotate-end)); }
			}
			.scrollbar-hide::-webkit-scrollbar { display: none; }
			.scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
		</style>

		<style>
			.tab-panel {
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background-color: white;
				border: 1px solid #e5e7eb;
				border-radius: 12px;
				overflow-y: auto;
				overflow-x: hidden;
				transform-style: preserve-3d;
				transform-origin: center -30px;
				transition: transform 0.7s cubic-bezier(0.4, 0, 0.2, 1);
				backface-visibility: hidden;
				scrollbar-width: none;
			}
			.tab-panel:not(#tab-cover) { background-color: white; }
			#tab-home { z-index: 4; }
			#tab-gallery { z-index: 3; }
			#tab-location { z-index: 2; }
			#tab-heart { z-index: 1; }
			.tab-panel.active { transform: rotateX(0deg); }
			.tab-panel.flipped { transform: rotateX(90deg); }
			.tab-panel.hidden-flipped { transform: rotateX(90deg); }
			.tab-panel.hidden-under { transform: rotateX(0deg); }
			.tab-btn { color: #9ca3af; transition: color 0.2s; }
			.tab-btn.active { color: #111827; }
			.map-tab { background-color: #f9fafb; color: #9ca3af; border: 1px solid #f3f4f6; }
			.map-tab.active { background-color: white; color: #111827; border-color: #e5e7eb; border-top-color: white; z-index: 5; }
			.trans-tab { background-color: white; color: #111827; border: none; }
			.trans-tab.active { background-color: #111827; color: white; }
			.notebook-rings { position: absolute; top: 10px; left: 10px; right: 10px; display: flex; justify-content: space-around; padding: 0 10%; z-index: 10; }
			.ring { width: 12px; height: 22px; border: 2px solid #e5e7eb; border-bottom: none; border-radius: 6px 6px 0 0; }
		</style>

		<script is:inline>
			document.addEventListener('DOMContentLoaded', function() {
				const tabBtns = document.querySelectorAll('.tab-btn');
				const tabPanels = document.querySelectorAll('.tab-panel');
				const tabOrder = ['home', 'gallery', 'location', 'heart'];
				let currentPage = 0;
				let isAnimating = false;
				let canFlip = false;

				function initPages() {
					tabPanels.forEach((panel) => {
						if (panel.id !== 'tab-cover') panel.classList.add('hidden-under');
					});
				}
				initPages();

				async function flipToPage(targetIndex) {
					if (isAnimating || !canFlip || targetIndex === currentPage) return;
					isAnimating = true;
					const animationDuration = 100;
					const stepDuration = 100;
					if (targetIndex > currentPage) {
						for (let i = currentPage; i < targetIndex; i++) {
							const pageToFlip = document.getElementById('tab-' + tabOrder[i]);
							pageToFlip.classList.remove('active', 'hidden-under');
							pageToFlip.classList.add('flipped');
							await new Promise(res => setTimeout(res, stepDuration));
						}
					} else {
						for (let i = currentPage - 1; i >= targetIndex; i--) {
							const pageToUnflip = document.getElementById('tab-' + tabOrder[i]);
							pageToUnflip.classList.remove('flipped', 'hidden-flipped');
							pageToUnflip.classList.add('active');
							await new Promise(res => setTimeout(res, stepDuration));
						}
					}

					await new Promise(res => setTimeout(res, animationDuration));
					currentPage = targetIndex;
					tabPanels.forEach((panel) => {
						if (panel.id === 'tab-cover') return;
						const panelIndex = parseInt(panel.dataset.page, 10);
						panel.classList.remove('active', 'flipped', 'hidden-flipped', 'hidden-under');
						if (panelIndex === currentPage) {
							panel.classList.add('active');
						} else if (panelIndex < currentPage) {
							panel.classList.add('hidden-flipped');
						} else {
							panel.classList.add('hidden-under');
						}
					});
					const targetPanel = document.getElementById('tab-' + tabOrder[targetIndex]);
					if(targetPanel) targetPanel.scrollTop = 0;
                    
                    if (tabOrder[targetIndex] === 'location') {
                        setTimeout(() => {
                            if (window.mapInstance) {
                                window.mapInstance.relayout();
                                window.mapInstance.setCenter(new kakao.maps.LatLng(37.2709214, 127.0661671));
                            }
                            
                            // 네이버 지도 레이지 로딩
                            const activeMapTab = document.querySelector('.map-tab.active');
                            if (activeMapTab && activeMapTab.dataset.map === 'naver') {
                                if (!window.naverMapInstance) {
                                    initNaverMap();
                                } else {
                                    window.naverMapInstance.updateBy(null, true);
                                    window.naverMapInstance.setCenter(new naver.maps.LatLng(37.2709214, 127.0661671));
                                }
                            }
                        }, 500);
                    }

					isAnimating = false;
				}

				tabBtns.forEach((btn, index) => {
					btn.addEventListener('click', function() {
						if (!canFlip) return;
						const targetTab = this.getAttribute('data-tab');
						const targetIndex = tabOrder.indexOf(targetTab);
						updateTabButtons(targetIndex);
						flipToPage(targetIndex);
					});
				});

				function updateTabButtons(index) {
					tabBtns.forEach((b, i) => {
						if (i === index) b.classList.add('active');
						else b.classList.remove('active');
					});
				}

                const mapTabs = document.querySelectorAll('.map-tab');
                const mapViews = document.querySelectorAll('.map-view');
                const routeBtn = document.getElementById('route-btn');
                const routeUrls = {
                    kakao: 'https://map.kakao.com/link/to/아이티컨벤션,37.2709214,127.0661671',
                    naver: 'https://map.naver.com/p/entry/place/33989425',
                    google: 'https://www.google.com/maps/dir/?api=1&destination=아이티컨벤션'
                };

                mapTabs.forEach(tab => {
                    tab.addEventListener('click', () => {
                        const target = tab.dataset.map;
                        mapTabs.forEach(t => t.classList.remove('active'));
                        tab.classList.add('active');
                        mapViews.forEach(v => v.style.display = 'none');
                        const targetView = document.getElementById('map-' + target);
                        if (targetView) targetView.style.display = 'block';
                        routeBtn.href = routeUrls[target];

                        if (target === 'kakao' && window.mapInstance) {
                            window.mapInstance.relayout();
                            window.mapInstance.setCenter(new kakao.maps.LatLng(37.2709214, 127.0661671));
                        }
                        if (target === 'naver') {
                            if (!window.naverMapInstance) {
                                initNaverMap();
                            } else {
                                setTimeout(() => {
                                    window.naverMapInstance.updateBy(null, true);
                                    window.naverMapInstance.setCenter(new naver.maps.LatLng(37.2709214, 127.0661671));
                                }, 50);
                            }
                        }
                    });
                });

                // 카카오맵 초기화 로직
                function initKakaoMap() {
                    var container = document.getElementById('map-kakao');
                    var options = {
                        center: new kakao.maps.LatLng(37.2709214, 127.0661671),
                        level: 3
                    };
                    window.mapInstance = new kakao.maps.Map(container, options);
                    var markerPosition = new kakao.maps.LatLng(37.2709214, 127.0661671);
                    var marker = new kakao.maps.Marker({ position: markerPosition });
                    marker.setMap(window.mapInstance);
                    var infowindow = new kakao.maps.InfoWindow({
                        content: '<div style="padding:5px;font-size:12px;text-align:center;width:150px;color:#000;background:#fff;border:1px solid #ccc;">아이티 컨벤션</div>'
                    });
                    infowindow.open(window.mapInstance, marker);
                }

                // 네이버 지도 초기화 로직
                function initNaverMap() {
                    const mapContainer = document.getElementById('map-naver');
                    if (!mapContainer || typeof naver === 'undefined' || !naver.maps) return;
                    if (mapContainer.offsetWidth === 0) {
                        setTimeout(initNaverMap, 100);
                        return;
                    }
                    const center = new naver.maps.LatLng(37.2709214, 127.0661671);
                    window.naverMapInstance = new naver.maps.Map(mapContainer, {
                        center: center,
                        zoom: 15,
                        autoResize: true
                    });
                    const marker = new naver.maps.Marker({ position: center, map: window.naverMapInstance });
                    const infowindow = new naver.maps.InfoWindow({
                        content: '<div style="padding:10px;font-size:12px;text-align:center;width:150px;color:#000;background:#fff;border:1px solid #ccc;border-radius:5px;">아이티 컨벤션</div>'
                    });
                    infowindow.open(window.naverMapInstance, marker);
                }

                if (typeof kakao !== 'undefined' && kakao.maps) {
                    kakao.maps.load(initKakaoMap);
                }

				let lastFlipTime = 0;
				const flipCooldown = 500;
				let galleryScrollEdge = null;

				function handleDirectionalFlip(direction) {
					const now = Date.now();
					if (isAnimating || !canFlip || (now - lastFlipTime < flipCooldown)) return;
					let targetIndex = currentPage;
					if (direction === 'next' && currentPage < tabOrder.length - 1) targetIndex++;
					else if (direction === 'prev' && currentPage > 0) targetIndex--;
					if (targetIndex !== currentPage) {
						lastFlipTime = now;
						galleryScrollEdge = null; // 페이지 넘김 시 플래그 초기화
						updateTabButtons(targetIndex);
						flipToPage(targetIndex);
					}
				}

				window.addEventListener('wheel', (e) => {
					if (!canFlip) return;
					if (e.target.closest('.map-view')) return;
					if (document.getElementById('contact-modal').classList.contains('pointer-events-auto')) return;
					if (document.getElementById('text-modal').classList.contains('pointer-events-auto')) return;
					
					const galleryTab = e.target.closest('#tab-gallery');
					if (galleryTab) {
						const scrollContainer = galleryTab.querySelector('.overflow-y-auto');
						if (scrollContainer && e.target.closest('.overflow-y-auto')) {
							const isAtTop = scrollContainer.scrollTop <= 2;
							const isAtBottom = Math.abs(scrollContainer.scrollHeight - scrollContainer.clientHeight - scrollContainer.scrollTop) < 5;
							if (e.deltaY < 0 && isAtTop) {
								if (galleryScrollEdge === 'top') handleDirectionalFlip('prev');
								else galleryScrollEdge = 'top';
								return;
							} else if (e.deltaY > 0 && isAtBottom) {
								if (galleryScrollEdge === 'bottom') handleDirectionalFlip('next');
								else galleryScrollEdge = 'bottom';
								return;
							}
							galleryScrollEdge = null;
							return;
						}
					}
					if (e.deltaY > 0) handleDirectionalFlip('next');
					else if (e.deltaY < 0) handleDirectionalFlip('prev');
				}, { passive: true });

				let touchStartY = 0;
				window.addEventListener('touchstart', (e) => {
					if (!canFlip) return;
					if (e.target.closest('.map-view')) return;
					if (document.getElementById('contact-modal').classList.contains('pointer-events-auto')) return;
					if (document.getElementById('text-modal').classList.contains('pointer-events-auto')) return;
					touchStartY = e.touches[0].clientY;
				}, { passive: true });

				window.addEventListener('touchend', (e) => {
					if (!canFlip) return;
					if (e.target.closest('.map-view')) return;
					if (document.getElementById('contact-modal').classList.contains('pointer-events-auto')) return;
					if (document.getElementById('text-modal').classList.contains('pointer-events-auto')) return;
					
					const touchEndY = e.changedTouches[0].clientY;
					const diff = touchStartY - touchEndY;
					if (Math.abs(diff) < 50) return;

					const galleryTab = e.target.closest('#tab-gallery');
					if (galleryTab) {
						const scrollContainer = galleryTab.querySelector('.overflow-y-auto');
						if (scrollContainer && e.target.closest('.overflow-y-auto')) {
							const isAtTop = scrollContainer.scrollTop <= 2;
							const isAtBottom = Math.abs(scrollContainer.scrollHeight - scrollContainer.clientHeight - scrollContainer.scrollTop) < 5;
							if (diff < 0 && isAtTop) {
								if (galleryScrollEdge === 'top') handleDirectionalFlip('prev');
								else galleryScrollEdge = 'top';
								return;
							} else if (diff > 0 && isAtBottom) {
								if (galleryScrollEdge === 'bottom') handleDirectionalFlip('next');
								else galleryScrollEdge = 'bottom';
								return;
							}
							galleryScrollEdge = null;
							return;
						}
					}
					if (diff > 0) handleDirectionalFlip('next');
					else if (diff < 0) handleDirectionalFlip('prev');
				}, { passive: true });

				// 복사 기능 공통 함수
				function copyToClipboard(text, btn) {
					navigator.clipboard.writeText(text).then(() => {
						const originalText = btn.innerText;
						btn.innerText = '완료';
						btn.classList.add('bg-gray-900', 'text-white');
						setTimeout(() => {
							btn.innerText = originalText;
							btn.classList.remove('bg-gray-900', 'text-white');
						}, 1500);
					});
				}

				// 연락처 모달 제어
				const modal = document.getElementById('contact-modal');
				const modalBox = document.getElementById('modal-content-box');
				const modalTitle = document.getElementById('modal-title');
				const modalPhone = document.getElementById('modal-phone');
				const modalAccount = document.getElementById('modal-account');
				const closeBtn = document.getElementById('modal-close-btn');

				function closeModal() {
					modal.classList.remove('opacity-100', 'pointer-events-auto');
					modal.classList.add('opacity-0', 'pointer-events-none');
					modalBox.classList.remove('scale-100');
					modalBox.classList.add('scale-90');
				}

				// 텍스트 모달 제어 (소개, 오시는길용)
				const textModal = document.getElementById('text-modal');
				const textModalBox = document.getElementById('text-modal-content-box');
				const textModalTitle = document.getElementById('text-modal-title');
				const textModalBody = document.getElementById('text-modal-body');
				const textModalCloseBtn = document.getElementById('text-modal-close-btn');

				function openTextModal(title, content) {
					textModalTitle.innerText = title;
					textModalBody.innerHTML = content;
					
					// 인사말의 경우 각 문장을 한 줄씩 표시하되, 화면보다 길면 자연스럽게 줄바꿈
					if (title === '인사말') {
						const spans = textModalBody.querySelectorAll('span');
						spans.forEach(s => {
							s.style.display = 'block';
							s.style.whiteSpace = 'normal';
							s.style.marginBottom = '10px';
						});
					}
					
					// 고정된 큰 글씨 크기 설정
					textModalBody.style.fontSize = '1.4rem';
					
					textModal.classList.remove('opacity-0', 'pointer-events-none');
					textModal.classList.add('opacity-100', 'pointer-events-auto');
					textModalBox.classList.remove('scale-90');
					textModalBox.classList.add('scale-100');
				}

				function closeTextModal() {
					textModal.classList.remove('opacity-100', 'pointer-events-auto');
					textModal.classList.add('opacity-0', 'pointer-events-none');
					textModalBox.classList.remove('scale-100');
					textModalBox.classList.add('scale-90');
				}

				// 클릭 핸들러 등록
				document.querySelectorAll('.contact-card').forEach(card => {
					card.addEventListener('click', (e) => {
						e.stopPropagation();
						const name = card.querySelector('.info-name').innerText;
						const phone = card.querySelector('.info-phone').innerText;
						const account = card.querySelector('.info-account').innerText;

						modalTitle.innerText = name;
						modalPhone.innerText = phone;
						modalAccount.innerText = account;

						modal.classList.remove('opacity-0', 'pointer-events-none');
						modal.classList.add('opacity-100', 'pointer-events-auto');
						modalBox.classList.remove('scale-90');
						modalBox.classList.add('scale-100');
					});
				});

				const introBox = document.getElementById('intro-box');
				if (introBox) {
					introBox.addEventListener('click', (e) => {
						e.stopPropagation();
						const content = introBox.querySelector('div').innerHTML;
						openTextModal('인사말', content);
					});
				}

				const venueBox = document.getElementById('venue-box');
				if (venueBox) {
					venueBox.addEventListener('click', (e) => {
						e.stopPropagation();
						const title = venueBox.querySelector('h4').innerText;
						const info = Array.from(venueBox.querySelectorAll('p')).map(p => p.innerText).join('<br>');
						openTextModal(title, info);
					});
				}

				// 교통 정보 탭 전환 (말풍선 유지)
				const transTabs = document.querySelectorAll('.trans-tab');
				const balloon = document.getElementById('trans-balloon');
				const balloonTail = document.getElementById('balloon-tail');
				const balloonContent = document.getElementById('balloon-content');
				
				const transTitles = { shuttle: "셔틀버스 안내", subway: "지하철 안내", parking: "주차 안내" };
				const transData = {
					shuttle: "청명역 5번 출구 셔틀버스 이용 (10분 소요)<br><br>첫 예식 기준 30분 간격 왕복 무료 운행 (정각/30분)",
					subway: "<b>수원역(1호선):</b><br>4번 출구 버스 환승(10, 10-2, 10-5, 37 등)<br><br><b>청명역(수인분당선):</b><br>5번 출구 셔틀버스 이용",
					parking: "흥덕IT밸리 단지 내 1,800대 동시 주차 가능<br><br>하객 분들께는 <b>2시간 무료 주차</b>를 제공합니다."
				};

				let currentTransType = null;

				transTabs.forEach(tab => {
					tab.addEventListener('click', (e) => {
						e.stopPropagation();
						const target = tab.dataset.trans;
						const isActive = tab.classList.contains('active');

						transTabs.forEach(t => t.classList.remove('active'));
						
						if (isActive) {
							balloon.classList.add('opacity-0', 'pointer-events-none', 'translate-y-2');
							currentTransType = null;
						} else {
							tab.classList.add('active');
							currentTransType = target;
							balloonContent.innerHTML = transData[target];
							
							// 말풍선 및 꼬리 위치 계산
							const tabRect = tab.getBoundingClientRect();
							const containerRect = tab.parentElement.getBoundingClientRect();
							const relativeLeft = tabRect.left - containerRect.left + (tabRect.width / 2);
							const percentLeft = (relativeLeft / containerRect.width) * 100;
							
							balloonTail.style.left = percentLeft + '%';
							balloon.classList.remove('opacity-0', 'pointer-events-none', 'translate-y-2');
						}
					});
				});

				// 말풍선 클릭 시 큰 모달 열기
				balloon.addEventListener('click', (e) => {
					e.stopPropagation();
					if (currentTransType) {
						openTextModal(transTitles[currentTransType], transData[currentTransType]);
					}
				});

				// 닫기 및 전역 클릭
				closeBtn.addEventListener('click', (e) => { e.stopPropagation(); closeModal(); });
				textModalCloseBtn.addEventListener('click', (e) => { e.stopPropagation(); closeTextModal(); });
				
				window.addEventListener('click', (e) => {
					if (modal.classList.contains('pointer-events-auto')) {
						if (!modalBox.contains(e.target) && !e.target.closest('.contact-card')) {
							closeModal();
						}
					}
					if (textModal.classList.contains('pointer-events-auto')) {
						if (!textModalBox.contains(e.target) && !e.target.closest('#intro-box') && !e.target.closest('#venue-box') && !e.target.closest('#trans-balloon')) {
							closeTextModal();
						}
					}
					
					// 오시는 길 말풍선 외부 클릭 시 닫기
					if (!e.target.closest('.trans-tab') && !e.target.closest('#trans-balloon')) {
						transTabs.forEach(t => t.classList.remove('active'));
						balloon.classList.add('opacity-0', 'pointer-events-none', 'translate-y-2');
						currentTransType = null;
					}
				});

				// 기타 로직
				document.querySelectorAll('.copy-btn, .modal-copy-btn').forEach(btn => {
					btn.addEventListener('click', (e) => {
						e.stopPropagation();
						const text = btn.dataset.copy || document.getElementById(btn.dataset.copyTarget)?.innerText;
						if (text) copyToClipboard(text, btn);
					});
				});

				const mainPhoto = document.getElementById('main-photo');
				// Removed click toggle to keep only image 42 visible as per user request

				const dDayDisplay = document.getElementById('d-day-display');
				if (dDayDisplay) {
					const targetDate = new Date('2026-08-22T11:00:00');
					const days = Math.ceil((targetDate - new Date()) / (1000 * 60 * 60 * 24));
					dDayDisplay.innerText = days > 0 ? `D-${days}` : days === 0 ? 'D-DAY' : `D+${Math.abs(days)}`;
				}

				window.addEventListener('curtain-animation-finished', () => {
					setTimeout(() => {
						const coverPage = document.getElementById('tab-cover');
						const homePage = document.getElementById('tab-home');
						if (coverPage && homePage) {
							coverPage.classList.remove('active');
							coverPage.classList.add('flipped');
							homePage.classList.remove('hidden-under');
							homePage.classList.add('active');
							canFlip = true;
						}
					}, 1400);
				});
			});
		</script>
		<script src="/chungchup/curtain-animation.js" is:inline></script>
	</body>
</html>